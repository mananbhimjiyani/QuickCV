import { useEffect, useState } from "react";

// ... existing code ...

try {
  // ... existing code ...
} catch {
  setError("Error fetching data!");
  setLoading(false);
}

// ... existing code ... 